services:
  azure-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: azure-server:latest
    env_file:
      - .env
    
    ports:
      - "8080:8080"
    
    restart: always
